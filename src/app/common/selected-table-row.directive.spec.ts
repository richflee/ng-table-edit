import { SelectedTableRowDirective } from './selected-table-row.directive';

describe('SelectedTableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedTableRowDirective();
    expect(directive).toBeTruthy();
  });
});
